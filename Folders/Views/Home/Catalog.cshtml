@using Folders.Data.DbModels
@model List<Folder>
<div>
    <div>
       <strong>
           @ViewBag.Name
       </strong>
    </div>
    @foreach(var a  in @Model)
    {
        <a class="navbar-brand" asp-controller="Home" asp-action="Catalog"  asp-route-path=@a.Path asp-route-name=@a.Name>@a.Name</a>
    }
</div>